this is a profanity plugin that shows what you are listening to right now via d-bus

requirements:
profanity
playerctl

TODO:
switch to dbus, instead of playerctl

installation:
make && make install 

(no need for root)

in profanity do
/plugin load profanity-playerctl.so

(only needed once)
